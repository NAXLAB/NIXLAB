hotkey-overlay {
    // Uncomment this line to disable the "Important Hotkeys" pop-up at startup.
    skip-at-startup
}

clipboard {
    disable-primary
}

window-rule {
  geometry-corner-radius 12
  clip-to-geometry true
}

gestures {
     hot-corners {
        top-left
    }
}

animations {
    overview-open-close {
        duration-ms 250
        curve "cubic-bezier" 0.25 0.8 0.2 1
    }
}


layer-rule {
    match namespace="^quickshell$"
    place-within-backdrop true
}

environment {
  XDG_CURRENT_DESKTOP "niri"
  QT_QPA_PLATFORM "wayland"
  ELECTRON_OZONE_PLATFORM_HINT "auto"
  QT_QPA_PLATFORMTHEME "gtk3"
  QT_QPA_PLATFORMTHEME_QT6 "gtk3"
}

layout {
    background-color "transparent"

    focus-ring {
        off
    }

    gaps 12  // gap between windows and screen edges

    default-column-width { proportion 0.5; }

    struts {
        left 48 
        right 48
        // top 64
        // bottom 64
    }
    insert-hint {
        color "#7295FF60"
    }
}

input {
    keyboard {
        xkb { }
    }

    mouse {
        accel-speed 0.1
        accel-profile "flat"
        scroll-factor 1.5
    }
}

overview {
    zoom 0.4
    workspace-shadow {
        off
     }
}

binds {

    Super+T { spawn "kgx"; } 
    Super+F { spawn "nautilus"; }

    Super+Shift+Q { quit; }

	Super+Left { focus-column-left; }
	Super+Right { focus-column-right; }
	Super+Up { focus-window-up; }
    Super+Down { focus-window-down; }

	Super+Shift+Left { move-column-left; }
	Super+Shift+Right { move-column-right; }	

    Shift+Super+S { spawn "sh" "-c" "pgrep -x slurp > /dev/null && pkill -x slurp || grim -g \"$(slurp -b 0E0F11C8 -c 0070F0FF -s 0E0F1100 -w 2)\" - | wl-copy"; }
    Super+S { screenshot-screen; }

}

// Add to the end of ~/.config/niri/config.kdl
//include "dms/colors.kdl"
//include "dms/layout.kdl"
//include "dms/alttab.kdl"
include "dms/binds.kdl"
include "dms/cursor.kdl"
include "dms/outputs.kdl"
